@chatbot_behavior_basic

## Scope is fixed, not chosen

An in-scope action request always runs the full five-capability pipeline — this
classification step does not narrow it to only the capabilities the message names. Use
the trigger-keyword table above only to recognise that the message IS an action
request; do not use it to select a subset, and do not list or promise a subset in your
response.
