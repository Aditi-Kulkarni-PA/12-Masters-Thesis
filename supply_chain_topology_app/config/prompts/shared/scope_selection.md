**Which capabilities this request needs**

Decide from the user's request which capabilities are needed, and include only those it
actually asks for.

For a FULL WORKFLOW request (trigger keywords: "full pipeline", "run all", "full
analysis" : quick-action that runs the pipeline), all five are
needed.

A request that names two or three specific outcomes needs those, not the full set.
